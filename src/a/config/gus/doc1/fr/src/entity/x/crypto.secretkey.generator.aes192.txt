@description
Génère une clé secrète AES de 192 bits.

@comment
Champ `KeyGenerator kg` partagé, dont l'état interne avance à chaque appel à `generateKey()` — d'où le `*`. Refactor possible : instancier `KeyGenerator` en variable locale dans `g()`, sur le modèle de `crypto.hash.md5` pour `MessageDigest`, pour supprimer l'état partagé et redevenir une entité réellement `+`.

@state
GENERATED

@sign
*G<SecretKey>

@tags
CRYPTO

@difficulty
1

@creation_date
20160115

@generator
claude-sonnet-5

@generated_time
20260824_203658
